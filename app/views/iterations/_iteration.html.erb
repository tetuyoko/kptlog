<%= link_to iteration.name, [iteration.project, iteration] %>
[ <%= iteration.status %> ]
(<%= iteration.start_date %> ~  <%= iteration.exit_date %>)

<%= link_to '削除', [iteration.project, iteration], confirm: 'ほんとに削除していいですか?', method: :delete %>

<hr />
