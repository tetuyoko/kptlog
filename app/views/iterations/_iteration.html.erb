<%= iteration.name %>
[ <%= iteration.status %> ]
(<%= iteration.start_date %> ~  <%= iteration.exit_date %>)

<p>
<%= link_to 'iterationを削除する', [iteration.project, iteration], confirm: 'ほんとに削除していいですか?', method: :delete %>
</p>

<hr />
